{
  "name": "mj-tiles-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "validate": "turbo run validate"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
